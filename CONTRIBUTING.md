# Contributing

Keep rules deterministic and fixture-backed. Avoid LLM-only checks unless they are optional and clearly separated from CI-safe validation.

Before opening a PR, run:

```bash
npm run validate
```
